services:
  - type: web
    name: workflow-intelligence-api
    env: python
    pythonVersion: 3.11
    buildCommand: bash build.sh
    startCommand: python -m uvicorn backend.main:app --host 0.0.0.0 --port $PORT
